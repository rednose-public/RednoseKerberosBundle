<?xml version="1.0" ?>

<container xmlns="http://symfony.com/schema/dic/services"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://symfony.com/schema/dic/services http://symfony.com/schema/dic/services/services-1.0.xsd">
    <parameters>
        <parameter key="security.authentication.listener.kerberos.class">Rednose\KerberosBundle\Security\KerberosAuthenticationListener</parameter>
        <parameter key="security.authentication.provider.kerberos.class">Rednose\KerberosBundle\Security\KerberosAuthenticationProvider</parameter>
    </parameters>

    <services>
        <service id="security.authentication.listener.kerberos" class="%security.authentication.listener.kerberos.class%" public="false" abstract="true">
            <tag name="monolog.logger" channel="security" />
            <argument type="service" id="security.context" />
            <argument type="service" id="security.authentication.manager" />
            <argument /> <!-- Provider-shared Key -->
            <argument /> <!-- Kerberos user -->
            <argument type="service" id="logger" on-invalid="null" />
            <argument type="service" id="event_dispatcher" on-invalid="null"/>
        </service>

        <service id="security.authentication.provider.kerberos" class="%security.authentication.provider.kerberos.class%" abstract="true" public="false">
            <argument /> <!-- User Provider -->
            <argument type="service" id="security.user_checker" />
        </service>

    </services>
</container>
